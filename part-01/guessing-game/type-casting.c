#include <stdio.h>

int main() {
    int a = 3;
    int b = 2;
    double points = (double) a / (double) b;
    printf("%f\n", points);

    double pi = 3.1415;
    int converted_pi = (int) pi;
    printf("%f %d\n", pi, converted_pi);
}
