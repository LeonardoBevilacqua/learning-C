#include <stdio.h>

int main() {
    char secrect_word[20];

    sprintf(secrect_word, "MELANCIA");

    int is_correct = 0;
    int was_hanged = 1;

    do {
    } while (!is_correct && !was_hanged);
}
